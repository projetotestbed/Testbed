#------------------------------------------------
#
# Load a binary file into micaz mote (type 1)
# Auxiliary file to define Timeout value
#
#------------------------------------------------

# TIMEOUT(in seconds) = (BinarySize / RATIO) + OFFSET
RATIO=800.0
OFFSET=2.0
